{
  "name": "timer-pie",
  "version": "1.0.1",
  "description": "Countdown clock timer",
  "scripts": {
    "pretest": "npm run build",
    "test": "node tests/clock.test.js",
    "build": "node scripts/build.js",
    "start": "python3 -m http.server 8080 -d src"
  },
  "devDependencies": {
    "canvas": "^2.11.2",
    "jsdom": "^24.0.0",
    "qrcode": "^1.5.4"
  }
}
